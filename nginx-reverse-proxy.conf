# Nginx reverse proxy configuration for Fuel Tracker
# Copy this to /etc/nginx/sites-available/fuel-tracker
# Then: sudo ln -s /etc/nginx/sites-available/fuel-tracker /etc/nginx/sites-enabled/
# Finally: sudo nginx -t && sudo systemctl reload nginx

# Main domain - Frontend application
server {
    listen 80;
    server_name fuel-tracker.logan27.store;

    # Redirect HTTP to HTTPS (uncomment after SSL is configured)
    # return 301 https://$server_name$request_uri;

    # Client max body size (for file uploads)
    client_max_body_size 20M;

    # Proxy to Docker frontend container on port 8080
    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# Note: api.fuel-tracker.logan27.store is configured separately
# If you need to update the API subdomain configuration, check your existing nginx config files

# HTTPS configuration for main domain (add after obtaining SSL certificate)
# server {
#     listen 443 ssl http2;
#     server_name fuel-tracker.logan27.store;
#
#     # SSL certificate paths (update after obtaining certificate)
#     ssl_certificate /etc/letsencrypt/live/fuel-tracker.logan27.store/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/fuel-tracker.logan27.store/privkey.pem;
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Client max body size
#     client_max_body_size 20M;
#
#     # Proxy to frontend container
#     location / {
#         proxy_pass http://localhost:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#
#         # Timeouts
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
